# Server-only Google Gemini API key. Leave blank for deterministic demo mode.
GEMINI_API_KEY=
# This value is browser-visible; use only as a lightweight personal access gate.
NEXT_PUBLIC_ACCESS_CODE=demo
