import {test,expect} from "@playwright/test";test("expands, creates, and ranks a demo list",async({page})=>{await page.goto("/");await page.getByLabel("Access code").fill("demo");await page.getByRole("button",{name:/enter/i}).click();await page.getByLabel("List topic").fill("potato dishes");await page.getByRole("button",{name:/build my list/i}).click();await expect(page.getByText("WHO MAKES",{exact:false})).toBeVisible();await expect(page.locator(".edit")).toHaveCount(10);await page.getByRole("button",{name:/generate 5 more/i}).click();await expect(page.locator(".edit")).toHaveCount(15);await page.getByRole("button",{name:/confirm/i}).click();await expect(page.getByRole("heading",{name:/UNRANKED/})).toBeVisible();await page.locator(".card select").first().selectOption("S");await expect(page.locator(".tier-S .card")).toHaveCount(1)});
