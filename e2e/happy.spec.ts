import {test,expect} from "@playwright/test";test("creates and ranks a demo list",async({page})=>{await page.goto("/");await page.getByLabel("Access code").fill("demo");await page.getByRole("button",{name:/enter/i}).click();await page.getByLabel("List topic").fill("potato dishes");await page.getByRole("button",{name:/build my list/i}).click();await expect(page.getByText("WHO MAKES",{exact:false})).toBeVisible();await page.getByRole("button",{name:/confirm/i}).click();await expect(page.getByRole("heading",{name:/UNRANKED/})).toBeVisible();await page.locator(".card select").first().selectOption("S");await expect(page.locator(".tier-S .card")).toHaveCount(1)});
