import {NextResponse} from "next/server";import {z} from "zod";
const schema=z.object({topic:z.string().max(100),item:z.string().max(100),style:z.string().max(300)});
export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid item"},{status:400});const key=process.env.GEMINI_API_KEY;if(!key){const label=encodeURIComponent(p.data.item);return NextResponse.json({url:`https://placehold.co/640x480/ede8da/171717?text=${label}`,mock:true})}const prompt=`Create one clean square ranking-card image of ${p.data.item}, for a ${p.data.topic} tier list. Coherent art direction: ${p.data.style}. No text.`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.1-flash-image:generateContent?key=${key}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{responseModalities:["IMAGE"]}})});if(!r.ok)return NextResponse.json({error:"Image failed"},{status:502});const j=await r.json();const part=j.candidates[0].content.parts.find((x:{inlineData?:unknown})=>x.inlineData);return NextResponse.json({url:`data:${part.inlineData.mimeType};base64,${part.inlineData.data}`})}
